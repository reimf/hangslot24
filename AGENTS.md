Always reread project files in a new task.
Always keep my changes.
Do not manually run the Typescript compiler, because it is run as a background build task.
Do not use abbreviations in variable and function names (e.g. use "button" instead of "btn").
